node_modules/
dist/
coverage/
*.tsbuildinfo
.notes/
# R10.9 throwaway harness build output (the harness itself IS committed;
# the batch gate deletes throwaway/ after arbiter sign-off).
throwaway/*/.build/
