/**
 * @mixpanel-headless/browser — browser-specific surface (R9.3): injectable
 * CredentialStore, redirect-based PKCE, oauth_token mode.
 *
 * Placeholder skeleton (TS-1); implemented in a later port batch.
 */

/** Package name constant exercised by the skeleton smoke test. */
export const BROWSER_PACKAGE_NAME = "@mixpanel-headless/browser";
