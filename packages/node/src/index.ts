/**
 * @mixpanel-headless/node — Node-specific surface (R9.2): TOML config, token
 * files, localhost OAuth callback, bridge file, env resolution.
 *
 * Placeholder skeleton (TS-1); implemented in a later port batch.
 */

/** Package name constant exercised by the skeleton smoke test. */
export const NODE_PACKAGE_NAME = "@mixpanel-headless/node";
