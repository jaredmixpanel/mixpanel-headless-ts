/**
 * This file was automatically generated by the schema4api generator.
 * DO NOT MODIFY IT BY HAND. Instead, run `npm run schema4api` to regenerate this file.
 */

/* eslint-disable @typescript-eslint/naming-convention */
/* eslint-disable @typescript-eslint/array-type */
/* eslint-disable @stylistic/ts/quotes */

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/list_realtime_destinations_response.py **/

/**
 * Request body for creating a real-time (event streaming) destination.
 */
export interface CreateRealtimeDestinationRequest {
  auth_type?: DestinationAuthType;
  credential?: RealtimeDestinationCredential | null;
  custom_headers?: {
    [k: string]: string;
  } | null;
  name: string;
  platform: string;
  webhook_url: string;
}

export type CreateRealtimeDestinationResponse = CreateRealtimeDestinationSuccessResponse | ErrorResponseModel;

export interface CreateRealtimeDestinationSuccessResponse {
  results: RealtimeDestinationItem;
  status?: 'ok';
}

/**
 * Request body for creating a real-time stream.
 */
export interface CreateRealtimeStreamRequest {
  description?: string | null;
  destination_id: string;
  event_filters?: RealtimeStreamEventFilterInput[] | null;
  name: string;
  payload_template?: RealtimeStreamPayloadTemplate | null;
}

export type CreateRealtimeStreamResponse = CreateRealtimeStreamSuccessResponse | ErrorResponseModel;

export interface CreateRealtimeStreamSuccessResponse {
  results: RealtimeStreamItem;
  status?: 'ok';
}

/**
 * Request body for deleting a real-time (event streaming) destination.
 */
export interface DeleteRealtimeDestinationRequest {
  destination_id: string;
}

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/enable_realtime_destination_request.py **/

/**
 * Request body for deleting a real-time stream.
 *
 * The actor (`deleted_by`) is bound server-side from the authenticated user and
 * is intentionally absent here: a client-supplied actor is never trusted.
 */
export interface DeleteRealtimeStreamRequest {
  stream_id: string;
}

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/delete_realtime_stream_response.py **/

export type DeleteRealtimeStreamResponse = DeleteRealtimeStreamSuccessResponse | ErrorResponseModel;

export interface DeleteRealtimeStreamSuccessResponse {
  results: RealtimeStreamItem;
  status?: 'ok';
}

/**
 * Outcome of a single real-time delivery attempt.
 *
 * Values mirror the integration-manager `DeliveryStatus` proto enum.
 */
export type DeliveryStatus = 'DELIVERY_STATUS_UNSPECIFIED' | 'DELIVERY_STATUS_SUCCESS' | 'DELIVERY_STATUS_FAILED';

/**
 * Real-time destination auth scheme.
 *
 * Values mirror the integration-manager `AuthType` proto enum.
 */
export type DestinationAuthType = 'AUTH_TYPE_NONE' | 'AUTH_TYPE_API_KEY' | 'AUTH_TYPE_BEARER';

/**
 * Real-time destination authentication status.
 *
 * Mirrors the integration-manager `DestinationStatus` proto enum.
 */
export type DestinationStatus =
  | 'DESTINATION_STATUS_UNSPECIFIED'
  | 'DESTINATION_STATUS_AUTHENTICATED'
  | 'DESTINATION_STATUS_AUTH_ERROR'
  | 'DESTINATION_STATUS_DISABLED';

/**
 * Request body for disabling a real-time (event streaming) destination.
 */
export interface DisableRealtimeDestinationRequest {
  destination_id: string;
}

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/disable_realtime_destination_response.py **/

export type DisableRealtimeDestinationResponse = DisableRealtimeDestinationSuccessResponse | ErrorResponseModel;

export interface DisableRealtimeDestinationSuccessResponse {
  results: RealtimeDestinationItem;
  status?: 'ok';
}

/**
 * Request body for disabling a real-time stream.
 */
export interface DisableRealtimeStreamRequest {
  stream_id: string;
}

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/disable_realtime_stream_response.py **/

export type DisableRealtimeStreamResponse = DisableRealtimeStreamSuccessResponse | ErrorResponseModel;

export interface DisableRealtimeStreamSuccessResponse {
  results: RealtimeStreamItem;
  status?: 'ok';
}

/**
 * Request body for enabling a real-time (event streaming) destination.
 */
export interface EnableRealtimeDestinationRequest {
  destination_id: string;
}

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/enable_realtime_destination_response.py **/

export type EnableRealtimeDestinationResponse = EnableRealtimeDestinationSuccessResponse | ErrorResponseModel;

export interface EnableRealtimeDestinationSuccessResponse {
  results: RealtimeDestinationItem;
  status?: 'ok';
}

/**
 * Request body for enabling a real-time stream.
 */
export interface EnableRealtimeStreamRequest {
  stream_id: string;
}

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/enable_realtime_stream_response.py **/

export type EnableRealtimeStreamResponse = EnableRealtimeStreamSuccessResponse | ErrorResponseModel;

export interface EnableRealtimeStreamSuccessResponse {
  results: RealtimeStreamItem;
  status?: 'ok';
}

/**
 * Standard template for non-2xx API responses.
 */
export interface ErrorResponseModel {
  details?: {
    [k: string]: any;
  } | null;
  error: string;
  status?: 'error';
  type?: string | null;
}
export type GetRealtimeDestinationResponse = ErrorResponseModel | GetRealtimeDestinationSuccessResponse;

export interface GetRealtimeDestinationSuccessResponse {
  results: RealtimeDestinationItem;
  status?: 'ok';
}

export type ListRealtimeDeliveryLogsResponse = ErrorResponseModel | ListRealtimeDeliveryLogsSuccessResponse;

export interface ListRealtimeDeliveryLogsSuccessResponse {
  results: RealtimeDeliveryLogPage;
  status?: 'ok';
}

export type ListRealtimeDestinationsResponse = ErrorResponseModel | ListRealtimeDestinationsSuccessResponse;

export interface ListRealtimeDestinationsSuccessResponse {
  results: RealtimeDestinationItem[];
  status?: 'ok';
}

export type ListRealtimeStreamsResponse = ErrorResponseModel | ListRealtimeStreamsSuccessResponse;

export interface ListRealtimeStreamsSuccessResponse {
  results: RealtimeStreamItem[];
  status?: 'ok';
}

/**
 * Real-time stream payload template type.
 *
 * Values mirror the integration-manager `PayloadTemplateType` proto enum.
 */
export type PayloadTemplateType =
  | 'PAYLOAD_TEMPLATE_TYPE_UNSPECIFIED'
  | 'PAYLOAD_TEMPLATE_TYPE_FORWARD_ALL'
  | 'PAYLOAD_TEMPLATE_TYPE_CUSTOM';

/**
 * A single delivery attempt for a real-time stream.
 *
 * Fully system-written and read-only; the delivery worker records each attempt.
 * `response_status_code` is 0 on a transport failure; `error_detail` is empty on
 * success.
 */
export interface RealtimeDeliveryLogItem {
  attempted_at: string | null;
  created_at: string | null;
  destination_id: string;
  error_detail: string;
  event_count: number;
  id: string;
  response_status_code: number;
  status: DeliveryStatus;
  stream_id: string;
}

/**
 * A page of delivery logs (newest-first) plus the opaque cursor for the next
 * page. `next_cursor` is null when there are no more rows; it is owned by
 * integration-manager and passed back verbatim to fetch the following page.
 */
export interface RealtimeDeliveryLogPage {
  logs: RealtimeDeliveryLogItem[];
  next_cursor: string | null;
}

/**
 * Plaintext credential supplied by the create form. The webapp serializes and
 * KMS-encrypts this before it crosses the integration-manager gRPC boundary.
 *
 * - Bearer auth uses `token`.
 * - API-key auth uses `header_name` + `key`.
 */
export interface RealtimeDestinationCredential {
  header_name?: string | null;
  key?: string | null;
  token?: string | null;
}

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/update_realtime_destination_response.py **/

/**
 * A single real-time destination row for the single-platform list view.
 *
 * Sourced from integration-manager; only non-sensitive fields are exposed
 * (the encrypted credential and vendor config are never returned).
 */
export interface RealtimeDestinationItem {
  auth_type: string | null;
  created_at: string | null;
  created_by: UserInfo | null;
  destination_type: string;
  id: string;
  is_enabled: boolean;
  modified_at: string | null;
  name: string;
  platform: string;
  status: DestinationStatus;
  webhook_url: string | null;
}

/**
 * A single event filter on a real-time stream, as returned to the client.
 *
 * `property_filters` are opaque JSON objects owned upstream by Lexicon; they
 * round-trip unchanged from the integration-manager `EventFilter` proto.
 */
export interface RealtimeStreamEventFilter {
  event_name: string;
  property_filters: {
    [k: string]: any;
  }[];
}

/**
 * An event filter supplied by the create/update form.
 *
 * `property_filters` are opaque JSON objects passed through unchanged to the
 * integration-manager `EventFilter` proto.
 */
export interface RealtimeStreamEventFilterInput {
  event_name: string;
  property_filters?:
    | {
        [k: string]: any;
      }[]
    | null;
}

/**
 * A single real-time stream row.
 *
 * Sourced from integration-manager; system-observed health fields are exposed
 * read-only alongside the user-authored config.
 */
export interface RealtimeStreamItem {
  created_at: string | null;
  created_by: UserInfo | null;
  description: string;
  destination_id: string;
  enabled: boolean;
  event_filters: RealtimeStreamEventFilter[];
  id: string;
  modified_at: string | null;
  name: string;
  payload_template: RealtimeStreamPayloadTemplate | null;
  stream_health: string;
  stream_health_detail: string;
  version: number;
}

/**
 * The payload template for a real-time stream.
 *
 * `template_type` mirrors the integration-manager `PayloadTemplateType` proto
 * enum name (e.g. `PAYLOAD_TEMPLATE_TYPE_FORWARD_ALL`). `template_body` is a
 * Liquid template string, set only when the type is `CUSTOM`.
 */
export interface RealtimeStreamPayloadTemplate {
  template_body: string;
  template_type: PayloadTemplateType;
}

/**
 * Request body for updating a real-time (event streaming) destination.
 *
 * Mirrors the create request minus the fields the update path does not honor:
 * `platform` (the destination type is immutable) and `webhook_url` (the server
 * merge overlays only name, custom_headers, and auth). Omitting them avoids
 * accepting a value the server would silently drop. A `credential` is sent only
 * when the user re-enters one; an omitted credential preserves the stored,
 * KMS-encrypted credential server-side.
 *
 * `auth_type` is optional so the request can distinguish leaving auth untouched
 * from changing it: omitting it (or sending null) preserves the stored auth and
 * credential; an explicit `AUTH_TYPE_NONE` removes auth; a credentialed type
 * sets/rotates it. This avoids silently stripping auth from a request that just
 * means to edit another field.
 *
 * `custom_headers` is write-only (never returned, since a header may carry a
 * secret such as basic auth). Omitting it (or sending null) preserves the stored
 * headers server-side; an explicit empty object clears them; a non-empty object
 * replaces them.
 */
export interface UpdateRealtimeDestinationRequest {
  auth_type?: DestinationAuthType | null;
  credential?: RealtimeDestinationCredential | null;
  custom_headers?: {
    [k: string]: string;
  } | null;
  destination_id: string;
  name: string;
}

export type UpdateRealtimeDestinationResponse = ErrorResponseModel | UpdateRealtimeDestinationSuccessResponse;

export interface UpdateRealtimeDestinationSuccessResponse {
  results: RealtimeDestinationItem;
  status?: 'ok';
}

/**
 * Request body for updating a real-time stream.
 */
export interface UpdateRealtimeStreamRequest {
  description?: string | null;
  event_filters?: RealtimeStreamEventFilterInput[] | null;
  name: string;
  payload_template?: RealtimeStreamPayloadTemplate | null;
  stream_id: string;
}

export type UpdateRealtimeStreamResponse = ErrorResponseModel | UpdateRealtimeStreamSuccessResponse;

export interface UpdateRealtimeStreamSuccessResponse {
  results: RealtimeStreamItem;
  status?: 'ok';
}

/**
 * Basic user information for UI display.
 */
export interface UserInfo {
  email: string;
  id: number;
  name: string;
}

export type ValidatePayloadTemplateResponse = ErrorResponseModel | ValidatePayloadTemplateSuccessResponse;

/**
 * Result of a successful payload-template validation.
 */
export interface ValidatePayloadTemplateResult {
  valid: boolean;
}

/** Generated from analytics/webapp/app_api/projects/integrations/realtime/__types__/enable_realtime_stream_request.py **/

export interface ValidatePayloadTemplateSuccessResponse {
  results: ValidatePayloadTemplateResult;
  status?: 'ok';
}
