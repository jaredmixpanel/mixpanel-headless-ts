/**
 * This file was automatically generated by the schema4api generator.
 * DO NOT MODIFY IT BY HAND. Instead, run `npm run schema4api` to regenerate this file.
 */

/* eslint-disable @typescript-eslint/naming-convention */
/* eslint-disable @typescript-eslint/array-type */
/* eslint-disable @stylistic/ts/quotes */

/** Generated from analytics/webapp/app_api/projects/agent_flows/__types__/list_agent_flow_response.py **/

/**
 * An agentic automation was activated.
 */
export interface AgentFlowActivatedEntity {
  action: 'activated';
  entity_id: string;
  entity_name: string;
  entity_type: 'agent_flow';
  payload: AgentFlowPayloadUpdatedPayload;
  type: 'agent_flow.activated';
}
/**
 * API response representation of an agent flow.
 *
 * Mirrors what `format_agent_flow_for_api_response` returns: AgentFlow-specific
 * fields plus the shared Content fields produced by `format_content_for_api_response`.
 */
export interface AgentFlowApiResponse {
  allow_staff_override?: boolean;
  can_pin?: boolean;
  can_share?: boolean;
  can_update_basic?: boolean;
  can_view?: boolean;
  content_environments_id?: number | null;
  content_type: 'agent-flows';
  created: string;
  creator_email?: string | null;
  creator_id?: number | null;
  creator_name?: string | null;
  deleted?: string | null;
  description?: string | null;
  frequency: AgentFlowFrequency | null;
  graph: AgentFlowGraph;
  id: string;
  is_favorited?: boolean | null;
  is_shared_with_project?: boolean;
  is_superadmin?: boolean;
  last_modified_by_email?: string | null;
  last_modified_by_id?: number | null;
  last_modified_by_name?: string | null;
  last_run: string | null;
  modified: string;
  name: string;
  pinned_date?: string | null;
  project_id: number;
  project_name?: string | null;
  status: AgentFlowStatus;
  tags?: string[] | null;
  type: AgentFlowType;
  user_id: number | null;
}
export interface AgentFlowAuditLog {
  created: string;
  entity:
    | AgentFlowActivatedEntity
    | AgentFlowCreatedEntity
    | AgentFlowDeletedEntity
    | AgentFlowPausedEntity
    | AgentFlowTriggeredEntity
    | AgentFlowUpdatedEntity;
  id: string;
  user_email?: string | null;
  user_id?: string | null;
}

export interface AgentFlowChartImageApiResponse {
  results: AgentFlowChartImageResults;
  status: 'ok';
}
export interface AgentFlowChartImageResults {
  signed_url: string;
}
export interface AgentFlowCreatePayload {
  description?: string | null;
  graph: AgentFlowGraph;
  name: string;
  tags?: string[] | null;
  type: AgentFlowType;
}
/**
 * A new agentic automation was created.
 */
export interface AgentFlowCreatedEntity {
  action: 'created';
  entity_id: string;
  entity_name: string;
  entity_type: 'agent_flow';
  payload: AgentFlowPayloadCreatedPayload;
  type: 'agent_flow.created';
}
/**
 * An agentic automation was deleted.
 */
export interface AgentFlowDeletedEntity {
  action: 'deleted';
  entity_id: string;
  entity_name: string;
  entity_type: 'agent_flow';
  payload: AgentFlowPayloadDeletedPayload;
  type: 'agent_flow.deleted';
}
export interface AgentFlowEdge {
  id: string;
  source: string;
  source_handle?: string | null;
  target: string;
}
/**
 * Cadence values for scheduled trigger nodes.
 */
export type AgentFlowFrequency = 'hour' | 'day' | 'week' | 'month';
export interface AgentFlowGraph {
  edges?: AgentFlowEdge[];
  /**
   * @minItems 1
   */
  nodes: [
    BranchNode | MetricAnalysisNode | NotificationNode | TriggerNode,
    ...(BranchNode | MetricAnalysisNode | NotificationNode | TriggerNode)[],
  ];
  schema_version?: number;
  trigger_node_id: string;
}
export interface AgentFlowHistoryOkResponse {
  pagination: CursorPaginationResponse;
  results: AgentFlowAuditLog[];
  status?: 'ok';
}

/**
 * An active agentic automation was paused.
 */
export interface AgentFlowPausedEntity {
  action: 'paused';
  entity_id: string;
  entity_name: string;
  entity_type: 'agent_flow';
  payload: AgentFlowPayloadUpdatedPayload;
  type: 'agent_flow.paused';
}

export interface AgentFlowPayload {
  description: string | null;
  frequency: string | null;
  graph: {
    [k: string]: any;
  };
  id: string;
  last_run: string | null;
  name: string;
  project_id: string;
  status: string;
  type: string;
  user_id: string | null;
}

export interface AgentFlowPayloadCreatedPayload {
  current: AgentFlowPayload;
  previous: null;
}

export interface AgentFlowPayloadDeletedPayload {
  current: null;
  previous: AgentFlowPayload;
}

export interface AgentFlowPayloadUpdatedPayload {
  current: AgentFlowPayload;
  previous: AgentFlowPayload;
}
export interface AgentFlowRunApiResponse {
  results: AgentFlowRunTriggerResults;
  status: 'ok';
}
export interface AgentFlowRunPayload {
  agent_flow_id: string;
  agent_flow_name: string;
  failed_nodes: {
    [k: string]: FailedNodeInfo | string;
  };
  skipped_nodes?: {
    [k: string]: SkippedNodeInfo | string;
  };
  source: string;
  succeeded_nodes: {
    [k: string]: SucceededNodeInfo | string;
  };
  task_id: string;
}

export interface AgentFlowRunStatusApiResponse {
  results: AgentFlowRunStatusResults;
  status: 'ok';
}
/**
 * Single progress event from RedisProgressSink.push.
 *
 * Wire shape comes from RedisProgressSink.push:
 *     {'type': str, 'content': str | dict, 'ts': float}
 */
export interface AgentFlowRunStatusMessage {
  content:
    | {
        [k: string]: any;
      }
    | string;
  ts: number;
  type: 'task_dispatched' | 'run_started' | 'node_started' | 'node_completed' | 'node_failed';
}
export interface AgentFlowRunStatusResults {
  error?: string | null;
  messages: AgentFlowRunStatusMessage[];
  status: 'running' | 'completed' | 'failed';
}

export interface AgentFlowRunTriggerResults {
  task_id: string;
}
/**
 * Recurrence detail for a scheduled Agent Flow, plus the phase anchor.
 *
 * Flat model; field interpretation is gated by the parent
 * `ScheduledTriggerData.frequency`:
 *   - `weekdays`     : daily/weekly day chips, OR monthly nth-weekday.
 *   - `week_of_month`: monthly nth-weekday (First..Fourth / Last).
 *   - `day_of_month` : monthly day of month mode (1..31 or -1=last day).
 */
export interface AgentFlowSchedule {
  /**
   * Monthly by day of month: 1..31 or -1 (last day of month).
   */
  day_of_month?: number | null;
  /**
   * Target hour of day (0..23, 24h).
   */
  hour?: number;
  /**
   * 'Repeat every N' (weeks for weekly, months for monthly).
   */
  interval?: number;
  /**
   * rrule dtstart: phase anchor and earliest occurrence.
   */
  start_date?: string;
  /**
   * Monthly by nth weekday: 1..4 (First..Fourth) or -1 (Last).
   */
  week_of_month?: number | null;
  /**
   * 0=Sun..6=Sat. Daily/weekly run days; also monthly nth week_of_month weekday.
   */
  weekdays?: number[] | null;
}

/** Generated from analytics/webapp/app_api/projects/agent_flows/__types__/agent_flow_run_response.py **/

export type AgentFlowStatus = 'draft' | 'active' | 'paused' | 'archived';

/**
 * An agentic automation run was triggered, either manually or based on the configured schedule.
 */
export interface AgentFlowTriggeredEntity {
  action: 'triggered';
  entity_id: string;
  entity_name: string;
  entity_type: 'agent_flow';
  payload: AgentFlowRunPayload;
  type: 'agent_flow.triggered';
}
/**
 * Template hint for the flow's intent. Drives FE editor selection and the
 * activation-time minimum-shape rule. Extend as more template types are added.
 */
export type AgentFlowType = 'metric_monitor';

/**
 * Payload for updating an existing agent flow.
 *
 * All fields are optional; the service layer enforces non-null
 * rules for `name` and `graph` at runtime since `request_schema` validates
 * against JSON Schema (model_validators don't run at request time).
 *
 * `description` accepts explicit null to clear the field.
 *
 * Note: status field is intentionally excluded; use /activate and /pause endpoints instead.
 */
export interface AgentFlowUpdatePayload {
  description?: string | null;
  graph?: AgentFlowGraph | null;
  name?: string | null;
  tags?: string[] | null;
}
/**
 * An existing agentic automation was updated.
 */
export interface AgentFlowUpdatedEntity {
  action: 'updated';
  entity_id: string;
  entity_name: string;
  entity_type: 'agent_flow';
  payload: AgentFlowPayloadUpdatedPayload;
  type: 'agent_flow.updated';
}

export interface ApiErrorResponse {
  error: string;
  status: 'error';
  [k: string]: any;
}

export interface BranchNode {
  data: BranchNodeData;
  id: string;
  run_condition?: 'all_succeeded' | 'always' | 'any_succeeded';
  type: 'branch';
}
export interface BranchNodeData {}
/**
 * Response model for cursor-based pagination, containing the cursors for the next and previous pages.
 */
export interface CursorPaginationResponse {
  /**
   * Cursor for the next page.
   */
  next_cursor?: string | null;
  /**
   * Cursor for the previous page.
   */
  previous_cursor?: string | null;
}
export interface DeleteAgentFlowApiResponse {
  results: DeletedAgentFlow;
  status: 'ok';
}
export type DeleteResponse = ApiErrorResponse | DeleteAgentFlowApiResponse;

export interface DeletedAgentFlow {
  id: string;
}

/** Generated from analytics/webapp/app_api/projects/agent_flows/__types__/single_agent_flow_response.py **/

export interface EmailNotificationData {
  channel: 'email';
  recipient: string;
}
/**
 * Standard template for non-2xx API responses.
 */
export interface ErrorResponseModel {
  details?: {
    [k: string]: any;
  } | null;
  error: string;
  status?: 'error';
  type?: string | null;
}

/** Generated from analytics/webapp/app_api/projects/agent_flows/__types__/agent_flow_chart_image_response.py **/

/**
 * Structured failure detail for one failed node.
 *
 * `node_type` is the failed node's type (an `AgentFlowNodeType` value). It is
 * recorded here so consumers can resolve the node's type straight from the log
 * rather than looking the node_id up in the flow's current graph, which is
 * fragile: a node_id that changed since the run was recorded would no longer
 * resolve.
 *
 * `stage` is a failure_stage value (from `AgentFlowFailureStage`, plus the
 * dynamic `{node_type}_handler_exception`); None when the failure predates
 * stage tracking or the handler didn't record one.
 *
 * Extend with additional per-failure metadata over time rather than adding
 * parallel maps to `AgentFlowRunPayload`.
 */
export interface FailedNodeInfo {
  message: string;
  node_type: string;
  stage?: string | null;
}

export type GetResponse = AgentFlowChartImageApiResponse | ApiErrorResponse;

export type JsonValue = any;
export interface ListAgentFlowApiResponse {
  results: AgentFlowApiResponse[];
  status: 'ok';
}

export interface MetricAnalysisNode {
  data: MetricAnalysisNodeData;
  id: string;
  run_condition?: 'all_succeeded' | 'always' | 'any_succeeded';
  type: 'metric_analysis';
}

/**
 * Metric query plus optional LLM context.
 *
 * `bookmark_params` stored as a loose dict to decouple from upstream
 * bookmark schema changes. Validated at execution time by
 * parser, not at save time.
 */
export interface MetricAnalysisNodeData {
  additional_context?: string;
  bookmark_params: {
    [k: string]: JsonValue;
  };
}
export interface NotificationNode {
  data: EmailNotificationData | SlackChannelNotificationData;
  id: string;
  run_condition?: 'all_succeeded' | 'always' | 'any_succeeded';
  type: 'notification';
}
export type PatchResponse = ApiErrorResponse | SingleAgentFlowApiResponse;

export type PostResponse = AgentFlowRunApiResponse | ApiErrorResponse;

export type ScheduledTriggerData = ScheduledTriggerData1;
export interface ScheduledTriggerData1 {
  frequency: AgentFlowFrequency;
  method: 'scheduled';
  schedule?: AgentFlowSchedule | null;
}

export interface SingleAgentFlowApiResponse {
  results: AgentFlowApiResponse;
  status: 'ok';
}

/**
 * Structured skip detail for one node. `reason` is the human-readable skip
 * reason; `node_type` mirrors `FailedNodeInfo.node_type` for symmetry.
 */
export interface SkippedNodeInfo {
  node_type: string;
  reason: string;
}

export interface SlackChannelNotificationData {
  channel: 'slack_channel';
  slack_channel_id: string;
  slack_channel_name: string;
  slack_team_id: string;
}
/**
 * Structured success detail for one node. `output` is the JSON-serialized
 * `NodeOutput` (opaque, node-type-specific — decoded by consumers). `node_type`
 * is carried here for the same reliability reason as `FailedNodeInfo.node_type`.
 */
export interface SucceededNodeInfo {
  node_type: string;
  output: string;
}

export interface TriggerNode {
  data: ScheduledTriggerData;
  id: string;
  run_condition?: 'all_succeeded' | 'always' | 'any_succeeded';
  type: 'trigger';
}
