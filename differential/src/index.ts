/**
 * differential — oracle-ts (JSON-RPC bridge, design D14) and referee
 * harnesses (design D15a). oracle-ts lands in TS-7; the ajv referee in TS-8.
 *
 * Placeholder skeleton (TS-1).
 */

/** Package name constant exercised by the skeleton smoke test. */
export const DIFFERENTIAL_PACKAGE_NAME = "@mixpanel-headless/differential";
